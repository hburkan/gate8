{
  "name": "@gate8/game-rules",
  "version": "0.1.0",
  "description": "Rule / condition engine types and evaluation primitives. Rule engine ships in Phase 11.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
